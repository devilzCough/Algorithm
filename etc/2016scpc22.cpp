#include <iostream>
#include <algorithm>

using namespace std;

int T, N;
int P[10005], Q[10005];
int dp[2][10005];

int main()
{
	scanf("%d", &T);
	while (T--) {
		scanf("%d", &N);
		for (int i = 0; i < N; i++)
			scanf("%d", &P[i]);
		for (int i = 0; i < N; i++)
			scanf("%d", &Q[i]);


	}

	return 0;
}
