#include <iostream>
#include <algorithm>

using namespace std;

int T;
int K, files[510] = {0};

int main()
{
	scanf("%d", &T);
	while(T--) {
		scanf("%d", &K);
		for (int i = 0; i < K; i++)
			scanf("%d", &files[i]);

		for (int i = 1; i < K; i++) {

		}
	}
	return 0;
}
