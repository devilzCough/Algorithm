#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int n;
int w[10001] = {0};

int main()
{
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
		scanf("%d", &w[i]);

	// tree edge : n - 1
	for (int i = 1; i < n; i++) {

	}
	return 0;
}
